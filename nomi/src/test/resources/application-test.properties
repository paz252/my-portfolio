# Disable or mock out the live data ingestion pathway for testing environments
server.port=8080
spring.application.name=nomi

nomi.data.source-path=classpath:docs/test-resume.md

# Force disable Spring AI GenAI Auto-configurations during tests
spring.ai.google.genai.api-key=mock-key
spring.ai.model.chat=none
spring.ai.model.embedding=none